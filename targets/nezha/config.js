// zool-theme · runtime configuration for Nezha.
// Edit this file next to index.html and reload the page; no rebuild needed.
// Every key is optional. Komari users configure the same options in the admin panel instead.
window.ZoolConfig = {
  // defaultAppearance: 'system', // 'system' | 'light' | 'dark'
  // defaultView: 'ledger', // 'ledger' | 'cards' — home page shape; visitors can switch
  // defaultDensity: 'comfortable', // 'comfortable' | 'compact' — ledger row height (ledger only)
  // defaultLanguage: 'auto', // 'auto' | 'en' | 'zh-CN' | 'zh-TW' | 'ja'
  // siteTitle: '', // overrides the site name from the dashboard settings
  // showOverview: true, // overview metrics on the home page
  // showCost: true, // monthly cost summary (needs billing data in the public note)
  // showRegions: true, // region filter row
  // showGroups: true, // offer the group filter
  // defaultGrouped: false, // default state of the visitor’s “Show by group” button
  // showBilling: true, // price / expiry on node cards
  // showOffline: true, // list offline nodes
  // hiddenNodes: [], // server ids to hide, e.g. ['12', '13']
  // hideAdminEntry: false, // hide the sign-in button for visitors
  // footerNote: '', // one line of text in the footer
}
